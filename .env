# Domain / Traefik — app runs at https://self.plumoai.com
DOMAIN_NAME=self.plumoai.com
SSL_EMAIL=admin@plumoai.com

# Service versions
MAIN_VERSION=v1.0.36
AUTH_VERSION=v1.0.80
API_VERSION=v1.0.73

# MongoDB (company service)
MONGODB_HOST=mongodb
MONGODB_PORT=27017

# Auth service — internal URL for company (hostname = Docker service name, no /api/auth path)
AUTH_SERVICE_URL=http://auth:3000
